version: "2"
run:
  tests: true
linters:
  enable:
    - misspell
    - revive
    - unconvert
    - whitespace
  settings:
    misspell:
      locale: US
    revive:
      rules:
        - name: exported
          disabled: true
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - errcheck
        path: _test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/agenticraptor/undo-anything
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
